import { arrayFilterBy } from '../../../helpers/array-filter-by';
import { module, test } from 'qunit';

module('Unit | Helper | array filter by');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayFilterBy(42);
  assert.ok(result);
});
