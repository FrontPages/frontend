import coverflowAnimator from '../../../utils/coverflow-animator';
import { module, test } from 'qunit';

module('Unit | Utility | coverflow animator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = coverflowAnimator();
  assert.ok(result);
});
