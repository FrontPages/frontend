<section class="mdl-grid">
  {{#each model as |headline| }}
    <article class="mdl-cell mdl-cell--3-col">
      <div class="mdl-card mdl-shadow--2dp">
        <div class="mdl-card__title">
          <h2 class="mdl-card__title-text">{{headline.title}}</h2>
        </div>
      </div>
    </article>
  {{/each}}
</section>
